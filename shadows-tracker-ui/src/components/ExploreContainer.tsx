import './ExploreContainer.css';

interface ContainerProps { }

const ExploreContainer: React.FC<ContainerProps> = () => {
  return (
    <div className="container">
        <h2><strong>SHADOW HIJACKERS'S D & E LOCATION TRACKER</strong></h2>
        <p>STARTED CODING WITH IONIC, CAPACITOR, REACT JS, GOLANG, MONGODB, WEB SOCKET, GIS SERVICE</p>
    </div>
  );
};

export default ExploreContainer;
